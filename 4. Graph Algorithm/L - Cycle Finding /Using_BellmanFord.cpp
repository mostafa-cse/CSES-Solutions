#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main() {
    int n, m;
    cin >> n >> m;

    vector<array<int, 3>> edge(m);
    for (auto &[u, v, wt] : edge) {
        cin >> u >> v >> wt;
    }
    vector<
    return 0;
}